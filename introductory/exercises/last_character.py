# If Python starts counting from zero, and len returns the number of characters
# in a string, what index expression will get the last character in the string name?
# (Note: we will see a simpler way to do this later.)
